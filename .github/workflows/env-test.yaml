name: ENV test

on:
  pull_request:
    branches:
      - workflow_test

jobs:
  test:
    runs-on: [self-hosted, macOS, X64]
    defaults:
      run:
        shell: bash
    steps:
      - name: print envs
        run: |
          echo "${LANG}"
          echo "${JAVA_HOME}"
          echo "trigger 1"